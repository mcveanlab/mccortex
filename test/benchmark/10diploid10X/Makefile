
# Usage: ../run-sample-pop.sh <genome.fa> <indivs> <ploidy> <kmer> <snps> <indels> <invs> <invlen> <readlen> <mpsize> <covg>

all:
	../run-sample-pop.sh smaller.fa 10 2 31 10 10 10 10 100 200 30 error

clean:
	rm -rf diploid* falsepos* reads* mask* genome* truth* gap_sizes* mp_sizes* pop.*

.PHONY: all clean
